{
  "name": "thompsonsed-opencode-workspace",
  "private": true,
  "scripts": {
    "build": "bunx ocx build . --out dist",
    "dev": "bun run build && bunx serve dist"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
